import { Form, FormDiv, HeroTitle } from './Hero'

const Hero = () => (
  <Form>
    <FormDiv>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormDiv>
  </Form>
)

export default Hero
